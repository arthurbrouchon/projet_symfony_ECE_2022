<script src="https://cdn.tailwindcss.com"></script>
<style>
      img:hover {
        animation: bounce 2s;
      }

      
</style>
<link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
  />
<div class="mx-2 my-2">
    <div class=" h-16 sticky top-0 backdrop-opacity-100 bg-gradient-to-r from-neutral-400 to-neutral-300 flex justify-between pl-4 ">
        <a href="{{path('homepage')}}">
            <div class=" text-3xl inline-block align-baseline self-center font-mono text-white mt-4">
                DISTRICT
            </div>
        </a>
        <form action="/search" method="post">
        <p class="mt-5"><input type="text" name="search" /></p>
 
        </form>
        {% for brand in allbrands %}
            <a href="{{path('brand_specific', {slug: brand.getId})}}">
                <p class="text-xl inline-block align-baseline self-center font-mono text-white mt-5">{{brand.getName}}</p>
            </a>
        {% endfor %}

        <div class=" text-xl inline-block align-baseline self-center pr-4">
            <a href="{{path('adminhomepage')}}"><img src="https://www.hrlact.org/wp-content/uploads/2020/12/generic-user-icon-300x300.jpg" class="h-12 w-12"></a>
        </div>
       
    </div>