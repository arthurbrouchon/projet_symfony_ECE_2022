{{ include('components/header/header.html.twig') }}


<div>
    
    <div class=" w-full h-96 my-2 bg-gradient-to-r from-neutral-200 to-neutral-200 flex px-4">
        <div class=" w-1/2 h-96 text-center flex">
            <div class=" h-2/3 self-center flex flex-col mx-auto">
                <p class=" text-3xl inline-block align-baseline self-center font-mono my-4"> NOTRE NOUVEAUTE</p>
                <p class=" text-3xl inline-block align-baseline self-center font-mono my-4">
                    LA {{isnewshoe.getBrand}} {{isnewshoe.getModel}} {{isnewshoe.getName}}
                </p>
                <div class=" text-3xl inline-block align-baseline self-center font-mono my-4 bg-black rounded-md px-2 text-neutral-200 animate__animated animate__pulse animate__infinite infinite">
                    <a href="{{ path('product_specific', {slug: isnewshoe.getId}) }}"> DECOUVREZ-LA </a>
                </div>
                </div>
            </div>
            
        <div class=" w-1/2 h-96 ">
        <a href="{{ path('product_specific', {slug: isnewshoe.getId}) }}"> <img src="{{isnewshoe.getUrl}}" > </a>
             
        </div>
    </div>


    
    <div class="  w-full  my-20 flex justify-around">
            <div class="flex flex-col justify-center align-center">
                <p class="  text-3xl inline-block align-baseline self-center font-mono my-4"> NOTRE MARQUE DU MOMENT:</p>
                <p class="  text-3xl inline-block align-baseline self-center font-mono my-4">
                    {{isnewbrand.getName}}
                </p>
                <a href="{{path('brand_specific', {slug: isnewbrand.getName})}}">
               <img src="{{isnewbrand.getUrl}}" class=" h-36 w-48 self-center ml-24  ">
                 </a>
                
            </div>
        <div class="grid grid-cols-2 gap-px">
        {% for product in allproducts %}
        {% if product.getBrand == isnewbrand.getName %}
        <div   >
            
            <a  href="{{ path('product_specific', {slug: product.getId}) }}"><img src="{{product.getUrl}}"  class="w-96 h-60 imgbounce"></a>
        </div>
        {% endif %}
        {% endfor %}
        </div>
    </div>

    <div class="w-full  my-20 bg-slate-400 flex flex-col">
        <div class="text center text-4xl py-4 mx-auto inline-block align-baseline self-center text-white">
            NOS 8 COUPS DE COEUR
        </div>
        <div class="grid grid-cols-4 gap-px  ">
            {% for product in allproducts %}
            {% if product.GetIsfav == "1" %}
            <div class=" ">
            
                <a href="{{ path('product_specific', {slug: product.getId}) }}"><img src="{{product.getUrl}}" class="w-fit h-fit"></a>
            </div>
            {% endif %}
            {% endfor %}
        </div>
    </div>

    <div class="w-full  my-20 bg-white flex flex-col">
        <div class="text center text-4xl py-4 mx-auto inline-block align-baseline self-center text-slate-800 mb-8">
            TOUS NOS PRODUITS
        </div>
        <div class="grid grid-cols-4 gap-px  ">
            {% for product in allproducts|reverse %}
            
            <div class=" ">
            
                <a href="{{ path('product_specific', {slug: product.getId}) }}"><img src="{{product.getUrl}}" class="w-fit h-fit"></a>
            </div>
            
            {% endfor %}
        </div>
    </div>
    
</div>

{{ include('components/footer/footer.html.twig') }}